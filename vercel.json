{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/client",
        "buildCommand": "npm run build"
      }
    },
    {
      "src": "api/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/donations/(.*)", "dest": "/api/donations/$1.js" },
    { "src": "/sitemap\.xml", "dest": "/api/server.js" },
    { "src": "/", "dest": "/api/server.js" },
    { "handle": "filesystem" },
    { "src": "(.*)", "dest": "/api/server.js" }
  ]
}
